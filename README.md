Конфигурация по умолчанию подгрудается из data.conf, имеющего слеующую структуру:

    [server]
          dbConnection = user:password@/db_name
          charset = utf8
          parseTime = True
          loc = Europe/Moscow
